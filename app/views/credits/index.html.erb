<h1>💸 Your credits, your money 💸</h1>

<div id="new_credit">
  <%= render "form", credit: Credit.new %>
</div>

<div id="stats">
  <%= render "stats", credits: @credits %>
</div>

<div>
  <span>Dealer</span>
  <span>Amount</span>
  <span>Notes</span>
</div>
<% if @credits.any? %>
  <div id="credits">
    <%= render @credits %>
  </div>
<% else %>
  No credits yet.
<% end %>
