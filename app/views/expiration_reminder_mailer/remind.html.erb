<h1>Credit Expiring Soon!</h1>
<p><%= "$#{@credit.amount} from #{@credit.dealer.name} expires in #{@cadence} days" %></p>
<p>
  Visit
  <%= link_to "your credits page",
    magic_link_emails_authenticate_url(
      token: @token,
      redirect_path: @redirect_path
    )
  %> to view more info.
</p>
