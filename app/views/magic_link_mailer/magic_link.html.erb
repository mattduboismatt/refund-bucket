<h1>Welcome!</h1>
<p>
  Use
  <%= link_to "this link",
    magic_link_emails_authenticate_url(
      token: @token,
      redirect_path: @redirect_path
    )
  %> to access the app.
  It expires in <%= Authentication::MagicLink::TokenGenerator::TOKEN_SHELF_LIFE %> minutes.
</p>
